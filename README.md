👉 Add real “timeline entry” tracking (who spoke when and for how long)
👉 Add a UI for tuning VAD thresholds (live adjustment)
👉 Persist sessions per user / export sessions to JSON
👉 Add a “delete profile” button per session profile