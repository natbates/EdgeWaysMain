// Linear mapping arrays for MFCC normalization (identity by default)
// Replace with learned values if available from Python reference
export const MFCC_MAPPINGS_A = Array(40).fill(1);
export const MFCC_MAPPINGS_B = Array(40).fill(0);
